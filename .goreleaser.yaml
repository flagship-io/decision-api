# GoReleaser v2 refuses to run a configuration without this line.
version: 2

builds:
  - 
    main: ./cmd/server
    ldflags:
      - -X github.com/flagship-io/decision-api/pkg/models.Version={{ .Tag }}
